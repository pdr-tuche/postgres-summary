delete from exlcuir linhas especificas de uma tabela

```SQL
delete from nome_tabela
where condiçoes


```

truncate table limpar os registros de uma tabela(dados), nao deleta as colunas da tabela

```SQL
truncate table nome_tabela
```
